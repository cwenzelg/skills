{
  "name": "skills",
  "version": "0.1.0",
  "private": true,
  "description": "Skill library for Christian Wenzel's agents: personal, shared, and per-company (Venture Labs and its ventures)",
  "type": "module",
  "scripts": {
    "manifest": "node tools/build-manifest.mjs",
    "check": "node tools/build-manifest.mjs --check",
    "upstream:add": "node tools/upstream.mjs add",
    "upstream:sync": "node tools/upstream.mjs sync",
    "upstream:update": "node tools/upstream.mjs update",
    "triggers": "node tools/trigger-eval.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
