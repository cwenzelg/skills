#!/bin/sh
# Regenerate manifest.json from the skill tree and stage it, so the committed index always
# matches the committed skills. Activate once per clone: git config core.hooksPath .githooks
set -e
cd "$(git rev-parse --show-toplevel)"
node tools/build-manifest.mjs
git add manifest.json
